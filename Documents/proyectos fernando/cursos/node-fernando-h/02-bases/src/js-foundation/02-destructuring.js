


    // console.log(process.env.PORT ?? 3000)


    const {  SHELL,  npm_config_init_module } = process.env;

    // console.table({SHELL, npm_config_init_module})

    const characters = ['flash', 'superman', 'green lanter', 'batman']

    const [, , , batman] = characters;

    // console.log(batman)